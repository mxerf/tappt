---
"@mxerf/tappt": patch
---

First release published through GitHub Actions with OIDC provenance. No runtime code changes since 0.1.0 — this version exists so downstream consumers can verify the npm package against a signed provenance attestation.
